apiVersion: v1
kind: Service
metadata:
  name: {{ template "promotions.fullname" . }}
  labels:
    app: {{ template "promotions.name" . }}
    chart: {{ template "promotions.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "promotions.name" . }}
    release: {{ .Release.Name }}
